name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout release
      uses: actions/checkout@v2

    - name: Archive drcloud
      run: tar cvfz ./debug-back-deploy.tar.gz *

   # Runs a single command using the runners shell 
    - name: Archive debug-back-deploy
      run: tar cvfz debug-back-deploy.tar.gz .

       
    - name: AWS configure credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.SECRET_KEY }}
        aws-region: ap-northeast-2

    - name: Upload to S3
      run: aws s3 cp --region ap-northeast-2 ./debug-back-deploy.tar.gz s3://my-github-action-lsm/archive/

    - name: Deploy with AWS CodeDeploy
      run: aws deploy create-deployment --application-name my-codedeploy-app --deployment-config-name CodeDeployDefault.OneAtATime --deployment-group-name my-codedeploy-app-group --s3-location bucket=github-action-codedeploy-s3,bundleType=tgz,key=archive/debug-back-deploy.tar.gz
